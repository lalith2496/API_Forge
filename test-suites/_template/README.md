# API name

Replace this folder name and title with your service (e.g. `kb-api`, `task-api`).

## Metadata

| Field | Value |
|-------|--------|
| **API** | `<service name>` |
| **Base URL** | `https://qa6-api2.sprinklr.com` |
| **Input source** | Postman / OpenAPI (pick one) |
| **Spec / collection version** | e.g. `postman-export-2026-09-01` |
| **Last generated** | YYYY-MM-DD |
| **Generated by** | `<name>` |
| **LLM provider** | Gemini / QA6 LLM |
| **Endpoints covered** | e.g. `POST /api/v3/task`, `GET /api/v3/task/{id}` |

## Run conventions

**Categories to run:** happy_path, auth, validation, boundary, security

**Categories to skip:** rfc_problem, rfc_semantics, rfc_cookies (unless this API returns RFC 7807 errors)

## Auth

- **Authorization:** Bearer access token (UI: Access token)
- **Key:** API key header (UI: API key) — if applicable

## USER_INPUT fields

List any tenant-specific fields the suite expects:

| Field | Example | Notes |
|-------|---------|-------|
| `contentIds` | `12345` | Required for GET article |

## Known issues / false failures

Document anything the team should ignore or re-generate after a fix:

- e.g. “TC-48 fails on RFC 7807 — API returns plain JSON errors”

## Files in this folder

| File | Description |
|------|-------------|
| `YYYY-MM-DD_qa6_*.json` | API Forge test suite export |
